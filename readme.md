# Blog App Using ReactJS and Django

The Project is divided into two parts namelt frontned and backend.
Frontend folders holds all the React Related files whereas Backend folders holds all Django related files.